Base surface container used for every panel, list row, and content block in the product.

```jsx
<Card padding="40px">
  <p>Any content</p>
</Card>
```

Never add a colored left border or drop shadow — flat fill + 1px mist border is the only card treatment in this system.
